class Pinko {
    constructor(x,y,body){
      var options = {
        isStatic:true
    }
        this.body = Bodies.circle(x,y,this.r,opyion);
        World.add(world,this.body);
  }
  
    display() {
      //this.body.position.x = mouseX;
      //this.body.position.y = mouseY;
      RADIUS(10);
      fill("white");
    }
  }